{% from "macros/tag.njk" import tag %}

{#
    notice(title, url, tagText, tagColor, tagVariant, date, extraClass, headingLevel)
    - title: Notice title
    - url: Link URL
    - tagText: Optional label (e.g. "DŮLEŽITÉ")
    - tagColor: "accent" (default) | "primary" | "dark" | "gold" | "gray"
    - tagVariant: "subtle" (default) | "solid" | "plain" | "outline"
    - date: Date string
    - extraClass: Optional extra classes on root
    - headingLevel: "h3" (default) | "h4" | "h2" | "div"
#}
{% macro notice(
    title,
    url="#",
    tagText="",
    tagColor="accent",
    tagVariant="subtle",
    date="",
    extraClass="",
    headingLevel="h3"
) -%}
    {%- set classes = ["c-notice", extraClass] | select | join(" ") -%}
    <article class="{{ classes }}">
        <a href="{{ url }}" class="c-notice__link" aria-label="{{ title }}">
            <div class="c-notice__content">
                {% if tagText or date %}
                    <div class="c-notice__meta">
                        {% if tagText %}
                            {{ tag(text=tagText, color=tagColor, variant=tagVariant, size="sm") }}
                        {% endif %}
                        {% if date %}
                            <time class="c-notice__date">{{ date }}</time>
                        {% endif %}
                    </div>
                {% endif %}
                <{{ headingLevel }} class="c-notice__title">{{ title }}</{{ headingLevel }}>
            </div>
            <svg class="c-notice__arrow" width="18" height="18" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                <path d="M6 3.5L10.5 8L6 12.5"/>
            </svg>
        </a>
    </article>
{%- endmacro %}
